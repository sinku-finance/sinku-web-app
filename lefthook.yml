pre-commit:
  commands:
    biome:
      glob: "src/**/*.{js,jsx,ts,tsx}"
      run: npx @biomejs/biome check --write {staged_files}
      stage_fixed: true
commit-msg:
  commands:
    "lint commit message":
      run: npx --no -- commitlint --edit "$1"
